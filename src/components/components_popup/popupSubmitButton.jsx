import React from "react";

const PopupSubmitButton = (props) => {
    return (
        <div className="popup__submit-button" onClick={props.onClick}>Submit</div>

    )
}
export default PopupSubmitButton